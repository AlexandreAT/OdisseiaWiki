import styled from 'styled-components';

export const RequiredMark = styled.span`
  color: var(--clearneonRed) !important;
  font-family: inherit;
  font-weight: 700;
  margin-left: 0.3em;
  text-shadow:
    0 0 4px var(--neonRed),
    0 0 8px rgba(255, 0, 70, 0.72);
`;
